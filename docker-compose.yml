services:
  backend-server:
    build:
      context: ./backend
      dockerfile: Dockerfile
    depends_on:
      - postgres
    ports:
      - "3000:3000"

  frontend-server:
    build:
      context: ./front
      dockerfile: Dockerfile
    depends_on:
      - backend-server
    ports:
      - "80:5173"

  postgres:
    image: postgres:alpine
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data: